//Below add a new event post in powershell
$body = @{
    calendar_id = 1
    title = "K8s Test Event"
    start_time = "2026-03-01 15:00:00"
    end_time = "2026-03-01 16:00:00"
    description = "Native PowerShell POST"
} | ConvertTo-Json

Invoke-RestMethod -Uri http://localhost:3000/events -Method Post -Body $body -ContentType "application/json"

//Password need to be converted to base64
[Convert]::ToBase64String([Text.Encoding]::UTF8.GetBytes("password"))

minikube -p minikube docker-env | Invoke-Expression

docker build -t nodejs-image-v1 .
kubectl apply -f deploy.yaml

//Enable port forwarding
kubectl port-forward service/calendar-api-service 3000:3000

Check existing images: docker images
Check pod status: kubectl get pods
Check all: kubectl get all